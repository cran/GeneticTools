* Include examples (will be done, as soon as we have access to a dataset that is used in 
   a current article under review). The dataset will then be published as a accompanying
   package

* Add an informative summary/plot for the MDR cross-validation